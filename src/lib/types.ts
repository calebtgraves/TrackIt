export type Streak = {
  id: string;
  name: string;
  goal: string;
  type: string;
  streakCount: number;
  totalCount: number;
  unit: string;
  totalQuantity: number;
  totalTime: number;
  reportType: string;
  totalInputs: number;
  lastChecked: Date;
  isComplete: boolean;
  streak: number;
};
